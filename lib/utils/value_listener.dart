import 'package:flutter/widgets.dart';

class ValueListener {
  static ValueNotifier<bool> isDark = ValueNotifier<bool>(true);
  //definir clase que vamos a estar notificando
  //definir donde la vamos a utilizar
}
